-- Historical marker retained because this migration is already registered in
-- existing databases. Its schema changes are versioned in
-- 20260807193000_align_runtime_schema, which is safe for both upgrade paths.
SELECT 1;
